name: jobable_dpbo_final_project
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.1 <4.0.0'

dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  logger: ^2.5.0
  path: ^1.8.3
  shared_preferences: ^2.3.4
  sqflite: ^2.3.0

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  sqflite_common_ffi: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
